matrix.h / matrix.cpp - библиотека с классами.
main.cpp - пример программы, создает две больших матрицы и записывает их в /tmp/matrix_input.txt (в перовй строке размерность матрицы, в следующей все элементы через пробел),
			затем выполняет параллельное умножение и записывает в /tmp/matrix_output.txt

tester.py - скрипт, считывающй /tmp/matrix_input.txt и /tmp/matrix_output.txt, затем выполняет умножение и сравнивает результат, выводит True, если ответы совпадают.